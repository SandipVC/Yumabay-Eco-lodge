rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Media files: readable by anyone, but writable only by the backend (Admin SDK)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
